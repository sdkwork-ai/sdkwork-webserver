# webserver-config

This package owns the webserver-config capability on the backend-admin surface. Its component contract links the canonical SDKWork standards; normative text is not duplicated locally.
