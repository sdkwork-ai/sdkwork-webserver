/**
 * Application-shell messages owned by the Web Server PC browser host.
 *
 * Scope: bootstrap status, runtime-configuration failure, and route-surface
 * loading states. Business copy belongs to the owning feature package
 * fragment, never here.
 */
export const webserverShellApplicationEnUs = {
  "shell.brand": "SDKWork Web Server",
  "shell.status.loadingRuntime": "Loading SDKWork Web Server",
  "shell.status.loadingWorkspace": "Loading workspace",
  "shell.error.runtimeUnavailable": "Runtime configuration could not be loaded.",
} as const;
