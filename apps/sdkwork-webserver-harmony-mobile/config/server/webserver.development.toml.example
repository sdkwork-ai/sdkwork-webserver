# Server profile template for the SDKWork Web Server HarmonyOS mobile surface.
# Shared deployment authority lives at ../../../etc/sdkwork.deployment.config.json.
[runtime]
target = "harmony-native"
profile = "development"
