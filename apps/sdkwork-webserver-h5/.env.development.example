# Private browser-development bootstrap input. Keep the tracked value blank.
# The credential-entry Vite plugin resolves the live token for the dev process;
# this value must never use a VITE_* key or enter public/runtime-env.json.
SDKWORK_ACCESS_TOKEN=
