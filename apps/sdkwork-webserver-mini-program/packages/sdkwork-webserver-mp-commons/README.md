# SDKWork Webserver Mini Program Commons

Domain-neutral primitives shared by every mini program capability package of the
SDKWork Web Server console: screen-state resolution, design tokens, and the
cross-package message catalog.

No SDK access, no platform globals, no routing, no feature logic
(`MINI_PROGRAM_APP_ARCHITECTURE_SPEC.md` §3).
