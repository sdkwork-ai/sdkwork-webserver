# Durable certificate operation worker (copy to .env on control plane host)

# Unique stable lease owner for this worker instance
SDKWORK_WEBSERVER_CERT_WORKER_ID=certificate-worker-dev-1

# Pending/retry operation polling interval in seconds (default 5)
SDKWORK_WEBSERVER_CERT_OPERATION_POLL_INTERVAL_SECS=5

# Due-renewal scheduling interval in seconds (default 3600 = 1 hour)
SDKWORK_WEBSERVER_CERT_RENEW_SCAN_INTERVAL_SECS=3600

# Days before not_after to trigger renewal (default 30)
SDKWORK_WEBSERVER_CERT_RENEW_BEFORE_DAYS=30

# Shared with the application runtime and ACME service
SDKWORK_WEBSERVER_ACME_DIRECTORY_URL=https://acme-staging-v02.api.letsencrypt.org/directory
SDKWORK_WEBSERVER_ACME_PROFILE=staging
SDKWORK_WEBSERVER_ACME_CONTACT_EMAIL=admin@localhost
SDKWORK_WEBSERVER_ACME_WEBROOT=/var/www/acme-challenge
SDKWORK_WEBSERVER_ACME_OPERATION_TIMEOUT_MS=180000
# Encrypted ACME account credentials directory; required in production-like
# environments so renewals reuse one CA account across restarts.
SDKWORK_WEBSERVER_ACME_ACCOUNT_ROOT=/var/lib/sdkwork/webserver/certs/accounts
SDKWORK_WEBSERVER_CERT_LIVE_ROOT=/etc/sdkwork/certs/letsencrypt
SDKWORK_WEBSERVER_NGINX_SITES_ROOT=/etc/nginx/sites-enabled/sdkwork

# Self-hosted TLS runtime distribution: after each successful certificate
# operation the worker projects the node's listener certificate bindings into
# the TLS material root and snapshot file consumed by the data plane.
SDKWORK_WEBSERVER_NODE_UUID=
SDKWORK_WEBSERVER_TLS_MATERIAL_ROOT=/var/lib/sdkwork/webserver/tls-materials
SDKWORK_WEBSERVER_TLS_RUNTIME_SNAPSHOT_FILE=/var/lib/sdkwork/webserver/tls-materials/tls-runtime.json
SDKWORK_WEBSERVER_TLS_SNAPSHOT_ALPN=h2,http/1.1
