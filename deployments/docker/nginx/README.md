# WSL operator nginx sites are generated by
# `deployments/docker/scripts/install-wsl-nginx.sh` into
# `/etc/nginx/sites-enabled/sdkwork/<domain>.conf` (NGINX_SPEC.md).
#
# Declarative production/cloud web server source of truth:
# `deployments/webserver/` (SDKWORK_WEBSERVER_SPEC.md).
#
# Do not reintroduce hand-authored dual-authority conf files here.
