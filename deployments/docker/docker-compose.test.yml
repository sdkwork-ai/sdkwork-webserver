# sdkwork-webserver — test environment (external dependencies).
#
# Standalone compose file for the test environment. Uses external
# PostgreSQL and Redis (WSL host-native or remote managed services).
#
# Port mapping:
#   Management: host 18888 -> container 3800
#   Public data plane: host 18898/28430 -> container 80/443
#   (host 80/443 is reserved for development by default; override to claim them)
# Domain: server-test.sdkwork.com
#
# Usage:
#   docker compose -f deployments/docker/docker-compose.test.yml \
#     --env-file deployments/docker/env/test.env up -d
#
#   docker compose -f deployments/docker/docker-compose.test.yml down

name: sdkwork-webserver-test

# Bounded json-file log rotation (DEPLOYMENT_SPEC.md §6 runtime hardening).
x-default-logging: &default-logging
  driver: json-file
  options:
    max-size: "50m"
    max-file: "3"

services:
  webserver:
    image: registry.sdkwork.com/apps/sdkwork-webserver-standalone:${SDKWORK_WEBSERVER_IMAGE_TAG:-0.1.0}
    container_name: sdkwork-webserver-test
    restart: unless-stopped
    logging: *default-logging
    cap_add:
      - NET_BIND_SERVICE
    ports:
      - "${SDKWORK_WEBSERVER_TEST_HOST_PORT:-18888}:3800"
      - "${SDKWORK_WEBSERVER_TEST_IMPORT_HTTP_HOST_PORT:-18898}:80"
      - "${SDKWORK_WEBSERVER_TEST_HTTPS_HOST_PORT:-28430}:443"
    env_file:
      - path: env/test.env
        required: false
    environment:
      SDKWORK_WEBSERVER_CONTAINER_HEALTH_PORT: "3800"
      SDKWORK_WEBSERVER_ENVIRONMENT: test
      SDKWORK_WEBSERVER_PROFILE_ID: standalone.test
      SDKWORK_WEBSERVER_IMPORT_LISTENER_PORTS: ${SDKWORK_WEBSERVER_IMPORT_LISTENER_PORTS:-}
      SDKWORK_DEPLOY_APP_ROOT: /app/share/sdkwork/deploy
      SDKWORK_SKILLS_APP_ROOT: /app/share/sdkwork/skills
      SDKWORK_MCP_APP_ROOT: /app/share/sdkwork/mcp
      SDKWORK_SPACE_ROOT: ${SDKWORK_SPACE_ROOT:-/opt/deploy}
      SDKWORK_SPACE_HOST_PATH: ${SDKWORK_SPACE_HOST_PATH:-/opt/deploy}
      SDKWORK_SPACE_CLONE_URL: ${SDKWORK_SPACE_CLONE_URL:-https://github.com/sdkwork-ai/sdkwork-space.git}
      SDKWORK_SPACE_CLONE_PULL: ${SDKWORK_SPACE_CLONE_PULL:-true}
      # Import every enabled sibling module's deployments/webserver/ from the
      # space checkout (SDKWORK_WEBSERVER_SPEC.md §17); SDKWORK_SPACE_MODULES
      # remains the explicit override.
      SDKWORK_SPACE_AUTO_DISCOVER: ${SDKWORK_SPACE_AUTO_DISCOVER:-true}
      SDKWORK_SPACE_MODULES: ${SDKWORK_SPACE_MODULES:-}
      SDKWORK_WEBSERVER_MODULE_IMPORT_REQUIRED: ${SDKWORK_WEBSERVER_MODULE_IMPORT_REQUIRED:-false}
      SDKWORK_WEBSERVER_MODULE_IMPORT_PROBE_UPSTREAMS: ${SDKWORK_WEBSERVER_MODULE_IMPORT_PROBE_UPSTREAMS:-false}
      SDKWORK_MODULE_API_GATEWAY_DEPLOYMENT: ${SDKWORK_MODULE_API_GATEWAY_DEPLOYMENT:-docker}
      SDKWORK_MODULE_API_GATEWAY_HOST: ${SDKWORK_MODULE_API_GATEWAY_HOST:-}
      SDKWORK_MODULE_API_GATEWAY_PORT: ${SDKWORK_MODULE_API_GATEWAY_PORT:-3900}
      SDKWORK_MODULE_API_GATEWAY_BINARY: ${SDKWORK_MODULE_API_GATEWAY_BINARY:-/app/bin/sdkwork-api-cloud-gateway}
      SDKWORK_MODULE_API_GATEWAY_INSTALL_ROOT: ${SDKWORK_MODULE_API_GATEWAY_INSTALL_ROOT:-/opt/sdkwork/api-gateway}
      SDKWORK_DEPLOY_SANDBOX_KEY: ${SDKWORK_DEPLOY_SANDBOX_KEY:-deploy.local.opt_deploy}
      SDKWORK_DEPLOY_SANDBOX_ROOT: ${SDKWORK_DEPLOY_SANDBOX_ROOT:-/opt/deploy}
      SDKWORK_CORS_ALLOWED_ORIGINS: ${SDKWORK_CORS_ALLOWED_ORIGINS:-http://server-test.sdkwork.com,http://localhost:18888,http://127.0.0.1:18888,app://dsh,app://birdcoder,app://sdkwork,app://dtupay,tauri://localhost,https://servicewechat.com,https://server-test.sdkwork.com,https://server-test.birdcoder.com,http://server-test.birdcoder.com,https://server-test.dtupay.com,http://server-test.dtupay.com,https://server-test.noaper.com,http://server-test.noaper.com,https://server-test.sdkwork.cn,http://server-test.sdkwork.cn,https://server-test.birdcoder.cn,http://server-test.birdcoder.cn,https://server-test.dtupay.cn,http://server-test.dtupay.cn,https://server-test.noaper.cn,http://server-test.noaper.cn,https://server-test.skubc.com,http://server-test.skubc.com,https://server-test.skubc.cn,http://server-test.skubc.cn,https://server-test.zowalk.com,http://server-test.zowalk.com,https://server-test.zowalk.cn,http://server-test.zowalk.cn,https://server-test.offer86.com,http://server-test.offer86.com,https://server-test.offer86.cn,http://server-test.offer86.cn,https://server-test.86offer.com,http://server-test.86offer.com,https://server-test.86offer.cn,http://server-test.86offer.cn,https://server-app-test.sdkwork.com,http://server-app-test.sdkwork.com,https://server-app-test.birdcoder.com,http://server-app-test.birdcoder.com,https://server-app-test.dtupay.com,http://server-app-test.dtupay.com,https://server-app-test.noaper.com,http://server-app-test.noaper.com,https://server-app-test.sdkwork.cn,http://server-app-test.sdkwork.cn,https://server-app-test.birdcoder.cn,http://server-app-test.birdcoder.cn,https://server-app-test.dtupay.cn,http://server-app-test.dtupay.cn,https://server-app-test.noaper.cn,http://server-app-test.noaper.cn,https://server-app-test.skubc.com,http://server-app-test.skubc.com,https://server-app-test.skubc.cn,http://server-app-test.skubc.cn,https://server-app-test.zowalk.com,http://server-app-test.zowalk.com,https://server-app-test.zowalk.cn,http://server-app-test.zowalk.cn,https://server-app-test.offer86.com,http://server-app-test.offer86.com,https://server-app-test.offer86.cn,http://server-app-test.offer86.cn,https://server-app-test.86offer.com,http://server-app-test.86offer.com,https://server-app-test.86offer.cn,http://server-app-test.86offer.cn,https://server-admin-test.sdkwork.com,http://server-admin-test.sdkwork.com,https://server-admin-test.birdcoder.com,http://server-admin-test.birdcoder.com,https://server-admin-test.dtupay.com,http://server-admin-test.dtupay.com,https://server-admin-test.noaper.com,http://server-admin-test.noaper.com,https://server-admin-test.sdkwork.cn,http://server-admin-test.sdkwork.cn,https://server-admin-test.birdcoder.cn,http://server-admin-test.birdcoder.cn,https://server-admin-test.dtupay.cn,http://server-admin-test.dtupay.cn,https://server-admin-test.noaper.cn,http://server-admin-test.noaper.cn,https://server-admin-test.skubc.com,http://server-admin-test.skubc.com,https://server-admin-test.skubc.cn,http://server-admin-test.skubc.cn,https://server-admin-test.zowalk.com,http://server-admin-test.zowalk.com,https://server-admin-test.zowalk.cn,http://server-admin-test.zowalk.cn,https://server-admin-test.offer86.com,http://server-admin-test.offer86.com,https://server-admin-test.offer86.cn,http://server-admin-test.offer86.cn,https://server-admin-test.86offer.com,http://server-admin-test.86offer.com,https://server-admin-test.86offer.cn,http://server-admin-test.86offer.cn}
      SDKWORK_DATABASE_ENGINE: postgresql
      SDKWORK_DATABASE_HOST: ${WEBSERVER_POSTGRES_HOST:-host.docker.internal}
      SDKWORK_DATABASE_PORT: ${WEBSERVER_POSTGRES_PORT:-5432}
      SDKWORK_DATABASE_NAME: ${WEBSERVER_POSTGRES_TEST_DB:-sdkwork_ai_test}
      SDKWORK_DATABASE_SCHEMA: ${WEBSERVER_POSTGRES_TEST_DB:-sdkwork_ai_test}
      SDKWORK_DATABASE_SCHEMA_FALLBACK_PUBLIC: "false"
      SDKWORK_DATABASE_USERNAME: ${WEBSERVER_POSTGRES_TEST_USER:-sdkwork_ai_test}
      SDKWORK_DATABASE_PASSWORD: ${WEBSERVER_POSTGRES_TEST_PASSWORD:-}
      SDKWORK_DATABASE_SSL_MODE: ${WEBSERVER_POSTGRES_SSL_MODE:-disable}
      SDKWORK_DATABASE_MAX_CONNECTIONS: "10"
      SDKWORK_WEBSERVER_REDIS_ENABLED: "true"
      SDKWORK_WEBSERVER_REDIS_HOST: ${WEBSERVER_REDIS_HOST:-host.docker.internal}
      SDKWORK_WEBSERVER_REDIS_PORT: ${WEBSERVER_REDIS_PORT:-6379}
      SDKWORK_WEBSERVER_REDIS_DATABASE: "1"
      SDKWORK_WEBSERVER_REDIS_TLS: ${WEBSERVER_REDIS_TLS:-false}
      SDKWORK_WEBSERVER_REDIS_PASSWORD: ${WEBSERVER_REDIS_PASSWORD:-}
      # Drive delivery cache (shared host directory, LRU website content cache).
      SDKWORK_DRIVE_WEBSITE_CACHE_ENABLED: ${SDKWORK_DRIVE_WEBSITE_CACHE_ENABLED:-true}
      SDKWORK_DRIVE_WEBSITE_CACHE_ROOT: ${SDKWORK_DRIVE_WEBSITE_CACHE_ROOT:-/opt/deploy/drive/website-cache}
      SDKWORK_DRIVE_WEBSITE_CACHE_ENVIRONMENT: test
      SDKWORK_DRIVE_WEBSITE_CACHE_MAX_TOTAL_BYTES: ${SDKWORK_DRIVE_WEBSITE_CACHE_MAX_TOTAL_BYTES:-8589934592}
      SDKWORK_DRIVE_WEBSITE_CACHE_MAX_ENTRIES: ${SDKWORK_DRIVE_WEBSITE_CACHE_MAX_ENTRIES:-100000}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "curl -fsS http://127.0.0.1:$${SDKWORK_WEBSERVER_CONTAINER_HEALTH_PORT:-3800}/healthz || exit 1",
        ]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 180s
    volumes:
      - webserver-secrets-test:/etc/sdkwork/webserver/secrets
      - webserver-data-test:/var/lib/sdkwork/webserver
      - ${SDKWORK_DRIVE_CACHE_HOST_PATH:-/opt/deploy/drive}:/opt/deploy/drive:rw
      - ${SDKWORK_SPACE_HOST_PATH:-/opt/deploy}:/opt/deploy:ro
      - ${SDKWORK_SPACE_CHECKOUT_HOST_PATH:-${SDKWORK_SPACE_HOST_PATH:-/opt/deploy}/sdkwork-space}:/opt/deploy/sdkwork-space:rw
      - ${SDKWORK_MODULE_API_GATEWAY_CHECKOUT_HOST_PATH:-../../../sdkwork-api-cloud-gateway}:/opt/deploy/sdkwork-space/sdkwork-api-cloud-gateway:ro
      - ./scripts/entrypoint-standalone.sh:/usr/local/bin/sdkwork-webserver-entrypoint:ro
      # Mount workspace-built gateway so imports.d/import.conf aggregator works
      # (registry 0.1.0 rejects multi-module full nginx sidecars).
      - ../../target/release/sdkwork-api-webserver-standalone-gateway:/app/bin/sdkwork-api-webserver-standalone-gateway:ro
      - ../../../sdkwork-skills/database:/app/share/sdkwork/skills/database:ro
      - ../../../sdkwork-mcp/database:/app/share/sdkwork/mcp/database:ro
      - ../../../sdkwork-deployments/database:/app/share/sdkwork/deploy/database:ro

volumes:
  webserver-secrets-test:
  webserver-data-test:
