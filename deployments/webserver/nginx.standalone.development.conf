# Generated by the SDKWork web server standard (SDKWORK_WEBSERVER_SPEC.md)
# from server.common.toml + server.standalone.toml; do not edit by hand.
user sdkwork;
worker_processes auto;
pid /run/sdkwork/webserver/webserver.pid;
error_log /var/log/sdkwork/webserver/webserver/error.log warn;
events {
    worker_connections 1024;
}
http {
    sendfile on;
    keepalive_timeout 75;
    client_max_body_size 1100m;
    server_tokens off;
    gzip on;
    upstream gateway {
        least_conn;
        keepalive 32;
        server 127.0.0.1:3800;
    }
    server {
        listen 80;
        server_name server-dev.sdkwork.com server-dev.birdcoder.com server-dev.dtupay.com server-dev.sdkwork.cn server-dev.birdcoder.cn server-dev.dtupay.cn server-app-dev.sdkwork.com server-app-dev.birdcoder.com server-app-dev.dtupay.com server-app-dev.sdkwork.cn server-app-dev.birdcoder.cn server-app-dev.dtupay.cn server-admin-dev.sdkwork.com server-admin-dev.birdcoder.com server-admin-dev.dtupay.com server-admin-dev.sdkwork.cn server-admin-dev.birdcoder.cn server-admin-dev.dtupay.cn;
        location /api/ {
            proxy_pass http://gateway;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_http_version 1.1;
        }
        location / {
            proxy_pass http://gateway;
            proxy_set_header Host $host;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_http_version 1.1;
            proxy_buffering off;
            proxy_read_timeout 300s;
        }
    }
}
