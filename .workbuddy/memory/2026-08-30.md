# 2026-08-30 SDKWork Web Server 打包验证与 CI Release

## 完成
- 本地打包验证全部通过（WSL Ubuntu-22.04 内）：
  - cargo build --workspace --release（5 个二进制，rust 1.98.0）
  - webserver-release.mjs package（archive 61.7MB + sha256 + SBOM 567 组件 + validate 440 entries）
  - deb test/production（41.5MB×2）validate 通过
  - rpm test/production（61MB×2）validate 通过
  - docker build（1.79GB 镜像，--skip-platform-gateway）
- 前端构建在 WSL 干净环境成功（PC/H5 standalone.production dist）
- 修复并提交（sdkwork-webserver @860b27ea）：
  - sdkwork.workflow.json：publish.githubRelease=true；dependencies 补 sdkwork-id/sdkwork-knowledgebase（cargo 链必需，cargo metadata 确认 11 仓库）；build 阶段加 workspace JS 库构建（sibling dist 不入库）；test target 加 variant=test 修正 id；preflight 装 cmake/clang/nasm/pkg-config/libssl-dev + cargo PATH 自适应定位；toolchains.rust 钉 1.98.0
  - .github/workflows/package.yml：framework_ref b0829529→bfe420b（旧版不支持 deploymentProfile/variant）；publish_release 仅 release 事件
  - pnpm-workspace.yaml：onlyBuiltDependencies: esbuild（pnpm 10 默认跳过 build script）
  - pnpm-lock.yaml：mcp/skills 依赖声明 +6
  - sdkwork.app.config.json：4 个安装包 checksum 更新（本地打包产出）
- sibling 修复并提交：sdkwork-mcp @a6f7543（mcp-pc-console-mcp 加 @sdkwork/utils）、sdkwork-skills @55d870d（skills-pc-admin-core 加 @sdkwork/skills-pc-core）
- tag v0.1.0 已推送（前移 3 次）

## CI 状态（卡点）
- Plan ✓（bfe420b 兼容）→ Preflight ✓ → Install ✓ → **Build 0-1 秒失败**（原因未知）
- 日志 403（repo 限制 admin），gh token 只读，无法看日志
- 本地所有环境（22.04/24.04 全新构建）cargo 编译均成功 → CI 失败为 runner 特有，需用户提供 Build 步骤日志
- tag run plan 的 "Require package artifacts for aggregate release" step 失败（fromJson(summary_json) 异常，main run 因 publish_release=false 短路通过）——已改 publish_release 仅 release 事件规避

## Docker 统一安装包 + 多实例（下午追加完成）
- 新增 `pnpm build:container:install` → scripts/docker/package-install-bundle.mjs：构建统一安装镜像并打包自包含 bundle（image.tar.gz+sha256、多实例 compose、env 模板、deploy.sh、manifest.json）
- 新增 `pnpm deploy:apply:standalone:docker` → deployments/docker/bundle/deploy.sh：--environment 必填、--replicas N 多实例、--external/--down/--purge/--ps/--logs/--dry-run，幂等
- 新增 deployments/docker/docker-compose.bundle.yml（环境中立多实例模板，每实例独立 compose 项目 sdkwork-webserver-<env>-i<i>）+ docker-compose.bundle-edge.yml（仅实例 1 发布 80/443）
- entrypoint-standalone.sh：node uuid 默认改为按容器主机名派生（多实例安全）
- sdkwork-space/bin 新增 build-webserver-docker.sh（空间级打包入口，WSL/git-bash 双路径探测）
- 规范完善：PNPM_SCRIPT_SPEC.md 新增 §4.4（统一安装包命令与多实例规则）；DEPLOYMENT_SPEC.md 新增 §6（容器安装镜像与多实例部署），Acceptance Checklist 顺延为 §7 并加 2 条
- 文档：docs/guides/operator/docker-install.md（中文）+ docker-install.en.md（英文）
- 验证：compose config 渲染通过（WSL docker compose v5.1.0）、deploy.sh dry-run 3 实例链路正确、deployment-contract.test 22/22、check-pnpm-script-standard 无新增告警
- 关键坑：compose 对 `:?required` 插值不分 profile → deps-only 命令也必须导出实例端口变量（deploy.sh 已处理）

## Docker bundle 合规修正（晚间追加）
- 依据 SDKWORK_WEBSERVER_SPEC.md 核对并修正 bundle：
  - §17 挂载语义：空间根 /opt/deploy 只读 + sdkwork-space checkout 子树 rw overlay（原为整体 rw）
  - §17.3：SDKWORK_WEBSERVER_IMPORT_PROFILE（默认 cloud）+ SDKWORK_WEBSERVER_IMPORT_LISTENER_PORTS 透传补齐
  - §17 多集群：容器内统一网关端口 3800；§17.4 standalone-only 打包链路确认（webserver-release.mjs standalone 产物）
  - §8.1 网关 upstream：模块 /api/ 反代走保留 upstream gateway；bundle 默认 bundled（3900）
- 新增多 webserver 独立配置：env/<environment>.i<N>.env 每实例覆盖文件，deploy.sh 以第二 --env-file 叠加（后者覆盖前者），down/ps/logs 同样生效
- DEPLOYMENT_SPEC §6 增补：空间挂载 ro/rw、import profile 默认 cloud、每实例 override 文件规则
- 文档 docker-install.md / .en.md 增补 §7 合规对照表 + §8 配置多个 webserver
- 验证：dry-run 显示 i2 叠加 override、i3 不叠加；compose 渲染 read_only: true 正确

## WSL 实机部署（晚间第二轮）
- 部署前清理：旧单容器栈（test/prod）占用目标端口 → docker rm 释放；镜像 registry.sdkwork.com/apps/sdkwork-webserver-standalone:0.1.0 复用（当日构建）
- 修复镜像内二进制不可执行：build-standalone-image.mjs copyTree 丢失 POSIX 执行位（writeFileSync 默认 0644）→ 加 chmodSync 保留 mode；Dockerfile 加 /app/bin 防御性 chmod；WSL 重建镜像 1m42s
- 修复 bundle compose postgres：缺 workspace 初始化（WEBSERVER_POSTGRES_DEV/TEST/PROD_* + ./postgres/init 挂载）→ 补齐后 dev/test 一次健康（20s）
- 新发现与修复：deploy.sh 增加网关 sibling（docker-compose.bundle-gateway.yml，服务名/别名 sdkwork-api-cloud-gateway，绑定 8080，规范 §17.3/§8.1）；网关 env 从规范 compose `docker compose config --format json` 提取（80 项，生成脚本 .sdkwork/runtime/gen-bundle-gateway.cjs）；kb-rpc 伴生容器 + 共享 gateway-secrets 卷；pgvector 扩展以超级用户预建入 workspace schema（002-enable-pgvector.sh）；postgres 镜像 postgres:16-alpine→pgvector/pgvector:pg16
- 网关 sibling 状态：pass 密钥供应/DB/vector/kb-rpc/IM outbox，**卡在 sdkwork-deploy-app-api 路由清单漂移**（manifest=105 vs OpenAPI=122，sdkwork-deployments 上游内容滞后）→ ensure_gateway 改为非阻塞（健康失败仅告警，module /api/ 504）
- 生产 webserver 崩溃：sdkwork-account 生产 sidecar 静态根 /usr/share/sdkwork/account/web/pc 容器内不存在（裸机路径）→ 需修 sdkwork-account deployments/webserver 生产 sidecar（兄弟模块）
- 最终状态：dev/test 全绿（mgmt 13800/18888 + 边缘 80/18898 均 200）；production 待修侧边车；HTTPS 443 需证书（ACME/DNS）
- api_gateway→api-gateway 路径规范修复：网关仓库 dist 陈旧产物（entrypoint/kb-rpc/metadata/安装脚本/service/manifest/INSTALL.md）全部 sed 修正为连字符；sdkwork-api-cloud-gateway:local 镜像重建（镜像内 entrypoint 0 下划线/8 连字符）；Rust loader 下划线仅为遗留回退（保留）

## 环境要点（Windows + WSL 打包）
- Windows node_modules 不可用于 WSL（win32 平台绑定 + symlink 指向错误 workspace），必须 WSL 干净环境 pnpm install
- cargo 在 /mnt/e（drvfs）编译有并发文件问题 → CARGO_TARGET_DIR 指向 WSL 原生 fs
- pnpm store 曾被 root 拥有 → wsl -u root chown 修复
- workflow framework：本地 sdkwork-github-workflow main=bfe420b；package.yml 原 pin b0829529 是旧版（无 deploymentProfile/variant 支持）
