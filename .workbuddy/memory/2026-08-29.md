# 2026-08-29 工作日志

## SDKWork 部署链路：WSL 打包 + Docker 部署包

### 关键环境事实
- WSL `Ubuntu-22.04`，root 可执行，Docker 29.3.0 运行中（`/run/docker.sock`）。
- `E:\sdkwork-space`（/mnt/e/sdkwork-space）是含 99 个 git 子模块的超级仓库。
- `/opt/deploy/sdkwork-space` 目前是指向 `/mnt/e/sdkwork-space` 的**符号链接**，需替换为真实拷贝目录。
- webserver 容器 import profile 已确认默认 **cloud**（`nginx.cloud.<env>.conf`），挂载 `/opt/deploy`。

### 已完成
1. `bin/pack-space-to-deploy.sh`：按 .gitignore 规范（git ls-files -c -o --exclude-standard，含 99 子模块）压缩工作区到时间戳归档，存 `/opt/deploy/archives/`，支持历史版本 + `--keep N` 裁剪。
   - 回归测试 + 真实环境验证；缺失文件用 tar `--ignore-failed-read` 降级跳过。
2. `bin/deploy-space-from-archive.sh`：staging 解压 → 校验 → 旧树 mv 到 `.previous-<ts>`（回滚）→ 原子替换。已回归验证。
3. `bin/package-docker-bundles.sh`：为 gateway/webserver 生成自包含 Docker 部署包（image.tar.gz + compose + env + deploy.sh/rollback.sh + README + manifest），外部 PG + Redis（无密码），webserver 挂载 /opt/deploy + cloud import profile。
4. `bin/docs/docker-bundle-layout.md`：部署包目录规范。

### 进行中
- 真实环境打包（98k 文件从 9p 挂载读取，约 25 分钟）。
- 之后：解压部署到 /opt/deploy/sdkwork-space（替换符号链接），再打包 gateway/webserver Docker bundles。

### 踩坑
- 9p/drvfs 挂载下 `du -sb` 递归扫描被忽略目录极慢 → 改为 `--size-estimate` 可选。
- Git Bash 下 `git -C`/`--file` 路径转换问题 → 脚本内用 `cd` 子 shell + 直接解析 .gitmodules 文本。
- `git ls-files` 会列出工作区已删除但仍 tracked 的文件 → tar `--ignore-failed-read` 处理。
