# REQ-2026-0057 Frozen Workspace And Bounded Release Archive Verification

```yaml
id: REQ-2026-0057
title: Freeze the release dependency graph and verify immutable server archives with bounded memory
owner: sdkwork-webserver
status: accepted
priority: P0
source: release-integrity-and-bounded-verification
problem: A release archive could be built from an incomplete or mutable pnpm workspace and uploaded after only checking its filename or outer checksum. That does not prove dependency closure, exact package inventory, safe tar metadata, deterministic identity, per-file integrity, or bounded verifier memory, and it leaves path traversal, link, archive bomb, and OOM risks at the publication boundary.
goals:
  - Make the application-root pnpm workspace and lockfile sufficient for a frozen release install after declared workflow dependencies are checked out.
  - Validate every generated standalone and cloud server archive before upload through a streaming, size-bounded parser.
  - Require an exact deterministic archive inventory, package manifest, file digest set, ownership, modes, timestamps, and outer checksum.
  - Reject unsafe, duplicate, unexpected, oversized, linked, special, or noncanonical archive entries before an artifact is trusted.
non_goals:
  - Claiming that the production Linux binaries, archive, installer, readiness probe, or HTTP/HTTPS traffic smoke ran on this Windows host.
  - Claiming Linux arm64, container, deb, rpm, Windows service, or macOS package support.
  - Claiming SBOM, signing, provenance, attestation, vulnerability, or license evidence that is not produced and verified by this requirement.
  - Extracting or executing an untrusted archive as part of archive validation.
users:
  - release engineer
  - platform operator
  - CI release workflow
acceptance_criteria:
  - pnpm install --frozen-lockfile succeeds from the application root after the sdkwork.workflow.json dependency set is present.
  - Native package build scripts are explicit: the required `esbuild` preparation script is allowlisted and no unexpected package-manager build-script warning remains after frozen install.
  - pnpm-workspace.yaml includes the application-owned packages and every sibling workspace package required by their workspace protocol dependencies; the lockfile is generated by the package manager and remains frozen during release validation.
  - The reusable workflow checks out the declared dependency repositories, packages a profile-specific artifact, validates that exact artifact, and only then uploads it.
  - Archive validation uses a mature tar parser, streams file hashing through a fixed 64 KiB read buffer, and never loads a package binary or the complete archive into memory.
  - The validator enforces a 512 MiB compressed archive limit, 256 MiB per-file limit, 1 GiB total declared content limit, 64-entry limit, 256 KiB manifest limit, and 256-byte checksum-sidecar limit.
  - The validator requires the exact file and directory inventory, deterministic path order, sourceDateEpoch timestamps, uid/gid 0, non-writable group/world modes, executable binaries, non-executable data files, and exact per-file sizes and SHA-256 values.
  - The validator rejects duplicate or unexpected paths, absolute or noncanonical paths, traversal, backslashes, NULs, symbolic links, hard links, devices, FIFOs, extended metadata, invalid ownership/modes, checksum mismatch, and manifest mismatch.
  - Package publication uses atomic archive and checksum replacement, validates the package immediately after creation, and exposes explicit standalone/cloud validation commands.
  - Black-box contract tests prove a valid archive and negative checksum, manifest hash, duplicate entry, unexpected entry, binary mode, and symbolic-link cases; platform-specific fixture limitations are reported as skips rather than claimed as passes.
non_functional_requirements:
  security: Archive input fails closed before trust, tracked package inputs contain no production secret, and no link or special entry can escape the declared package inventory.
  privacy: The package inventory contains application identity, schema, safe configuration examples, and binaries only; it contains no user or tenant data.
  performance: Validation memory is bounded independently of archive size by streaming entry reads and fixed metadata/manifests limits.
  reliability: Package identity is profile/version specific, writes are atomic and fsynced where supported, subprocesses have finite timeouts, and validation runs before upload.
affected_surfaces:
  - release
  - workflow
  - dependency-management
  - server-package
trace:
  specs:
    - REQUIREMENTS_SPEC.md
    - DEPENDENCY_MANAGEMENT_SPEC.md
    - DEPLOYMENT_SPEC.md
    - GITHUB_WORKFLOW_SPEC.md
    - RELEASE_SPEC.md
    - SUPPLY_CHAIN_SECURITY_SPEC.md
    - QUALITY_GATE_SPEC.md
    - TEST_SPEC.md
  components:
    - pnpm-workspace.yaml
    - pnpm-lock.yaml
    - sdkwork.workflow.json
    - .github/workflows/package.yml
    - scripts/webserver-release.mjs
    - tests/contract/release-archive.contract.test.mjs
verification:
  - pnpm install --frozen-lockfile
  - node --test tests/contract/release-archive.contract.test.mjs tests/contract/deployment-profile-commands.contract.test.mjs
  - node ../sdkwork-specs/tools/check-workspace-member-protocol.mjs --root .
  - node ../sdkwork-github-workflow/scripts/sdkwork-workflow.mjs validate --config sdkwork.workflow.json
  - node ../sdkwork-github-workflow/scripts/sdkwork-workflow.mjs matrix --config sdkwork.workflow.json --json
  - node ../sdkwork-specs/tools/check-pnpm-script-standard.mjs --root . --product-prefix web
  - pnpm verify
```

## Frozen Dependency Boundary

The application-root `pnpm-workspace.yaml` is the native package workspace authority used after the
reusable workflow checks out `sdkwork.workflow.json` dependencies as siblings. The root lockfile is
generated by the package manager and the release install uses `--frozen-lockfile`; CI must fail rather
than mutate dependency resolution. Workflow dependency declarations remain checkout authority and do
not replace Cargo or package-manager manifests.

The frozen install proves only that the declared source graph can be resolved reproducibly in the
assembled workspace. The root workspace explicitly allowlists the trusted `esbuild` preparation
script required by the Vite build graph; new native build scripts must be reviewed and added
explicitly rather than being silently executed. This is not an SBOM, vulnerability scan, license
review, signature, provenance, or proof that a mutable dependency ref resolved to the intended commit.

## Archive Trust Boundary

`scripts/webserver-release.mjs package` produces a deterministic profile/version-specific candidate,
atomically publishes the archive and checksum, and immediately calls the same validator used by the
explicit `validate` operation. The workflow also invokes validation as a distinct lifecycle step
before upload so a missing, replaced, or malformed artifact fails publication.

Validation does not extract the archive. It first bounds and verifies the outer archive and checksum,
then streams tar entries through the pinned `tar` parser. Every entry must match the exact package
contract and the bounded `package.manifest.json`. Entry paths, types, metadata, ownership, modes,
timestamps, declared/observed byte counts, per-file hashes, and ordering are verified before trust.

## Verification Evidence

- `pnpm install --frozen-lockfile` resolves all 24 workspace projects from the current lockfile
  without a resolution update, and the required `esbuild` build script is explicitly allowlisted.
- The paired release/profile contract run passes 11 tests with no failures; the NTFS symbolic-link
  fixture is explicitly skipped on Windows because it cannot provide portable tar link evidence.
- A real small tar fixture built through the same parser passes validation; checksum, manifest,
  duplicate/unexpected-entry, and executable-mode corruptions fail closed.
- Workspace member protocol, workflow schema/matrix, pnpm script, agent/workflow, repository docs,
  source configuration, and diff checks pass.
- `pnpm verify` passes after the parallel data-plane test port allocator was made process-unique;
  the complete 55-test data-plane integration suite also passes in four additional repeated runs.
- Windows non-dry-run packaging stops at the selected Linux architecture host gate before Cargo
  build or source repair.
- This requirement's acceptance evidence did not execute Linux archives. REQ-2026-0058 subsequently
  produced and validated the standalone/cloud Linux x64 archives and exercised packaged
  HTTP/HTTPS readiness, traffic, SIGTERM exit, and cleanup.

## Remaining Release Gates

This requirement closes dependency-resolution and archive-validation integrity only. Commercial
release still requires declared container/service targets, artifact
SBOM/signature/provenance/attestation,
vulnerability and license review, upgrade/rollback/uninstall evidence, and release approval.
